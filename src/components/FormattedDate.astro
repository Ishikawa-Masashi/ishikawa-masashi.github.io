---
interface Props {
  date: Date;
}

const date = Astro.props.date;
---

<time datetime={date.toUTCString()}>
  {
    date.toLocaleDateString("en-us", {
      year: "numeric",
      month: "numeric",
      day: "numeric",
    })
  }
</time>
